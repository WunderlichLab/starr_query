<VirtualHost *:80>
    ServerName fly-immune-enhancers.wunderlichlab.org

    # Point Apache at the WSGI entry point
    WSGIScriptAlias / /var/www/starr_query/starr_query.wsgi

    WSGIPassAuthorization On

    <Directory /var/www/starr_query>
        Require all granted
        Options -Indexes
    </Directory>

    # Serve static files directly via Apache (faster — bypasses Python entirely)
    Alias /static/ /var/www/starr_query/static/
    <Directory /var/www/starr_query/static>
        Require all granted
        Options -Indexes
        Header set Cache-Control "max-age=604800, public"
    </Directory>

    # Logs are visible via `docker logs` in BU's environment
    ErrorLog  ${APACHE_LOG_DIR}/starr_query_error.log
    CustomLog ${APACHE_LOG_DIR}/starr_query_access.log combined
</VirtualHost>
